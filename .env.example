# =========================================================
# IKA — frontend environment
# Copy this file to `.env` and set your backend URL.
# Without it, the app runs on built-in mock data (design mode).
# =========================================================

# Base URL of your backend (no trailing slash), e.g.
# VITE_API_BASE_URL=https://api.ika.example
VITE_API_BASE_URL=
